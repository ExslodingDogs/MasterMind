const Discord = require('discord.js');
const prefixes = require(`../botsettings/guildsettings.json`);

exports.run = (client, message, args) => {
    
    if(message.author.id === message.guild.owner.id){
        


    }


}

module.exports.help = {
  name: "setprefix"
}